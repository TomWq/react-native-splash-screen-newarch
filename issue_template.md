Run `npx react-native info` or `npx expo-doctor` in your project and share the output.

What `react-native-splash-screen-newarch` version are you using?

What React Native version are you using?

Are you using Expo? If yes, share:
- Expo SDK version
- Whether you use Expo Go, development client, or prebuild
- Your `plugins` entry for `react-native-splash-screen-newarch`

Is New Architecture enabled? (yes/no)

What platform does your issue occur on? (Android/iOS/Both)

Describe your issue as precisely as possible:
1. Steps to reproduce the issue
2. Expected behavior
3. Actual behavior
4. Relevant logs

Share screenshots or videos from a simulator or device if the issue is visual.

Show the code you are using:
- JavaScript/TypeScript `SplashScreen.hide()` call
- Android `MainActivity` changes, if not using the Expo plugin
- iOS `AppDelegate` changes, if not using the Expo plugin
